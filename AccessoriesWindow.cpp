#include "AccessoriesWindow.h"
/*
AccessoriesWindow::AccessoriesWindow(gameDataRef data, SoundManage *sounds, int type) : data(data), sounds(sounds), windowType(type)
{
}

void AccessoriesWindow::init()
{
	for (int i = 0; i < 2; i++)
	{
		data->assets.loadTexture("Baby Food " + std::to_string(i), "graphics/Effects/" + std::to_string(i) + ".png");
		fireWorksSpriteAnimaton.push_back(data->assets.getTexture("Fireworks " + std::to_string(i)));
	}
	for (int i = 2; i < 6; i++)
	{

	}
	for (int i = 0; i < 4; i++)
	{

	}
}
*/